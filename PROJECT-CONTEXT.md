# سياق المشروع

> اقرأ هذا الملف قبل أي تعديل. هو مصدر السياق الوحيد — لا تعتمد على نسخ
> قديمة كانت تُلصَق يدويًا في المحادثات، فأغلبها لم يعد صحيحًا.

آخر تحديث: 12 أغسطس 2026

---

## ما هو

نظام ERP عربي متعدد الشركات، منشور ويعمل على
`https://inventory-sync-system.onrender.com`.

مالك النظام يبيعه لعملاء. كل عميل شركة، وكل شركة عالم مغلق لا يرى غيره.

pnpm monorepo. React 19 + Express 5 + Postgres 18 (Neon) + Drizzle.
‏34 صفحة، 26 ملف راوت، 35 جدولًا.

```
artifacts/api-server/     Express — يُبنى بـ esbuild إلى dist/index.mjs
artifacts/store-system/   React + Vite + Tailwind v4 + wouter + TanStack Query
lib/db/                   مخططات Drizzle + الهجرات
lib/api-client-react/     عميل مولّد من OpenAPI — لا يُحرَّر يدويًا
tests/                    107 اختبار من طرف إلى طرف
tools/                    سكربتات بناء تعمل على ويندوز ولينكس
```

---

## العزل — كيف يعمل، ولماذا لا تلمسه

العزل مفروض من **قاعدة البيانات** لا من الاستعلامات. 33 جدولًا عليها
`ENABLE` + `FORCE ROW LEVEL SECURITY`، وسياسة واحدة تقارن `company_id`
بمتغيّر يضعه الخادم لكل طلب.

السبب: 296 استعلامًا في 26 ملفًا. أمانٌ يعتمد على تذكّر كتابة فلتر في كل
استعلام جديد يسقط أول مرة يُنسى فيها واحد.

**السلسلة كاملة:**

1. `middlewares/company-scope.ts` يفتح معاملة لكل طلب ويضع فيها هوية الشركة
   بـ `set_config('app.company_id', …, true)`.
2. `lib/db/src/index.ts` يوجّه كل استعلام إلى معاملة الطلب عبر
   `AsyncLocalStorage`، فلا يحتاج أي راوت تعديلًا.
3. سياسات RLS ترفض ما ليس للشركة، والعمود `company_id` يتعبّأ تلقائيًا في كل
   إدراج من نفس المتغيّر.

### ثلاثة أشياء تكسر هذا بصمت

**`SET` بدل `SET LOCAL`.** Neon يشغّل PgBouncer في وضع transaction: الاتصال
يعود للمخزن بعد كل معاملة ويُسلَّم لطلب آخر. القيمة المضبوطة بـ `SET` العادي
تبقى عليه فيرثها الطلب التالي — أي أن عميلًا يقرأ بيانات عميل آخر. **لا يظهر
هذا في اختبار محلي ولا في طلب واحد.** يمسكه اختبار التزامن في
`tests/isolation.test.mjs` وحده.

**حساب قاعدة بيانات يتجاوز RLS.** التطبيق يتصل بـ `app_user` وهو
`NOSUPERUSER NOBYPASSRLS` ويملك الجداول (ولهذا `FORCE` ضرورية). حساب Neon
الأصلي `neondb_owner` له `BYPASSRLS` — لو عاد `DATABASE_URL` إليه فالعزل يختفي
تمامًا والنظام يبدو سليمًا. يفحص `lib/verify-isolation.ts` هذا عند كل إقلاع
ويكتب في السجلات، ولا يمنع الإقلاع (خدمة لا تقوم لا يمكن تشخيصها).

**`TRUNCATE`.** لا يخضع لـ RLS إطلاقًا. كان `backup/reset` يستخدمه، فكانت
ضغطة واحدة من أدمن أي شركة تمسح بيانات الشركات كلها. استُبدل بـ `DELETE`.
**لا تُدخل `TRUNCATE` إلى أي مسار.**

### مسارات تعمل خارج النطاق عمدًا

`/healthz` و `/auth/login` و `/auth/me` و `/auth/signup` و `/auth/activate`
تسبق الميدلوير وتستخدم `rootDb` — لا جلسة لها ولا شركة. أي مسار آخر يجب أن
يأتي **بعد** `router.use(companyScope)` في `routes/index.ts`.

---

## الأدوار

`owner` ← `admin` ← `cashier`

- **المالك** بلا شركة (`company_id IS NULL`)، يرى الكل، ويبدّل بين الشركات من
  الزر أسفل الشريط الجانبي. الشركة المختارة في **الجلسة على الخادم** لا يرسلها
  العميل. وهو لا يكتب بيانات قبل اختيار شركة (409) وإلا وُلدت بلا شركة ولم
  يرها أحد.
- **أدمن الشركة** سيد شركته: بياناتها، نسختها الاحتياطية، مستخدموها. حساب
  المالك يختفي من قائمته تمامًا — RLS تفعل ذلك وحدها.
- **الكاشير** قائمة سماح صريحة في `middlewares/require-auth.ts`. أي مسار جديد
  ممنوع عليه تلقائيًا حتى يُسمح به. لا يرى سعر التكلفة (محذوف من رد الخادم لا
  مخفي في الشاشة).

---

## التسجيل الذاتي

مساران يفترقان بسؤال واحد: هل لك شركة قائمة؟

| | الموظف | العميل الجديد |
|---|---|---|
| يكتب | كود انضمام شركته | اسم شركته |
| الطلب يصل إلى | أدمن تلك الشركة | مالك النظام وحده |
| عند الموافقة | ينضم للشركة | تُنشأ الشركة ويصير أدمنها |

لا كلمة مرور عند التسجيل. الموافقة تُصدر كودًا من ٦ حروف يُرسَل بالبريد
(Brevo) **ويُعرَض للأدمن أيضًا** — فشل البريد يجب ألا يترك أحدًا محبوسًا خارج
حسابه. الكود مخزَّن مبصومًا، يموت بعد أسبوع، ولا يُستخدم مرتين.

الدخول يقبل البريد أو اسم المستخدم، ويرفض حسابات الشركات الموقوفة.

---

## الاختبارات

```bash
cd tests && npm install && npm test
```

‏107 اختبار عبر HTTP إلى خادم شغّال. اقرأ `tests/README.md` لتجهيز البيئة.
**لا تُسلّم تعديلًا دون تشغيلها.**

هي خارج مساحة pnpm وتُثبَّت بـ npm عمدًا: يجب ألا تكون سببًا في تغيير
`pnpm-lock.yaml` الذي يبني منه Render.

---

## الهجرات

مكتوبة يدويًا في `lib/db/migrations/` مع مدخل في `meta/_journal.json`
(‏`idx` متسلسل، `tag` = اسم الملف بلا امتداد).

‏`drizzle-kit generate` **يعمل الآن** — كان يفشل بسبب `pnpm-workspace.yaml`
لا بسبب ويندوز — لكن ملفات `meta/*_snapshot.json` لم تعد تطابق الواقع، فما
يولّده لن يكون صحيحًا. اكتبها يدويًا.

‏`db:migrate` يقرأ الـ journal فقط، ويعمل على Render ضمن أمر التشغيل.

---

## النشر

| | |
|---|---|
| الاستضافة | Render — Free، Frankfurt، Node 24، نشر تلقائي عند push |
| قاعدة البيانات | Neon Postgres 18 — فرع `production`، اتصال pooled |
| البناء | `corepack enable && pnpm install --frozen-lockfile && pnpm run build:deploy` |
| التشغيل | `pnpm run db:migrate && pnpm run start` |
| المتغيرات | `NODE_ENV` · `NODE_VERSION` · `SESSION_SECRET` · `DATABASE_URL` · `BREVO_API_KEY` · `BREVO_SENDER_EMAIL` · `BREVO_SENDER_NAME` |

**‏`DATABASE_URL` يجب أن يشير إلى `app_user`.** إعادته إلى `neondb_owner` تلغي
العزل بالكامل.

**قيود الخطة المجانية:** Render ينام بعد ١٥ دقيقة (أول طلب بعدها ~٥٠ ثانية —
مزعج لنقطة بيع)، Neon ‏٠٫٥ جيجابايت. **لا نسخ احتياطي تلقائي في أيٍّ منهما:**
`GET /api/backup/export` هو الوسيلة الوحيدة ويُشغَّل يدويًا. أول عميل يدفع =
وقت الترقية.

---

## أعطال وقعت فعلًا — لا تكررها

**رفع دون بناء أسقط الواجهة كلها.** ‏23 صفحة كانت تكتب
`fetch().then(r => r.json())` دون فحص `r.ok`، فأول رفض 403 مرّر جسم الخطأ
كبيانات وانهارت شجرة React. العلاج: `lib/http.ts` (`jsonOrThrow`) و
`error-boundary.tsx`. **أي رفض جديد يجب اختباره بحساب غير أدمن.**

**شاشة الإعدادات كانت تُنزّل دور المالك صامتًا** لأن قائمة الأدوار لم تحتوِ
"مالك". الحماية الآن في الخادم (`rejectOwnerEscalation`).

**ترقيم بـ `COUNT(*) + 1`** أوقف الفوترة نهائيًا: بعد حذف فاتورة يعيد رقمًا
مستخدَمًا فيصطدم بقيد التفرّد. كل المستندات تستخدم الآن
`lib/document-number.ts` (أكبر رقم + 1)، والترقيم مستقل لكل شركة.

**ردود الـ API كانت قابلة للتخزين في المتصفح** (ETag بلا `Cache-Control`)،
فكان المالك يبدّل الشركة ويرى قائمة الشركة السابقة من الذاكرة. الآن
`no-store` على كل `/api`.

**افحص الحقائق قبل الافتراض.** أكثر من مرة شُخِّص العطل خطأً: النشر بدا فاشلًا
وكان السبب دورًا مُنزَّلًا، والعزل بدا مكسورًا وكان `DATABASE_URL` لم يُحفظ.
استعلام واحد أو سطر سجل حسم كل مرة.

**بعد تعديل دور في قاعدة البيانات مباشرة، لا بد من خروج ودخول** — الجلسة تحمل
الدور القديم. (‏`/auth/me` صار يزامن الدور، لكن التبديل الكامل أضمن.)

---

## ما لم يُنفَّذ بعد

- **إعادة المستودع private** — كان public مؤقتًا.
- **ترقية الاستضافة** قبل أول عميل يدفع.
- نسخ احتياطي تلقائي — لا يوجد شيء منه اليوم.
- الواجهة فيها ~٢٥ خطأ TypeScript قديمًا (`pnpm run typecheck` يفشل عليها).
  البناء يمر لأن Vite لا يفحص الأنواع. لا تضف إليها.
